import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ruchita.habittracker',
  appName: 'HABIT TRACKER',
  webDir: 'build'
};

export default config;
